# Models

This folder contains the data models for the backend server. Models define the structure of the data and interact with the database.

## Files

- `userModel.ts`: Defines the user schema and methods.
- `videoModel.ts`: Defines the video schema and methods.
- `sessionModel.ts`: Defines the session schema and methods.

## Usage

Models should be used to interact with the database and perform CRUD operations.
