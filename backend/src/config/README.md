# Config

This folder contains configuration files for the backend server. These files include environment variables, database configurations, and other settings required for the application to run properly.

## Files
- `default.json`: Default configuration settings.
- `production.json`: Production-specific settings.
- `development.json`: Development-specific settings.

## Usage
Ensure that the appropriate configuration file is loaded based on the environment the server is running in.