# Components

This folder contains reusable React components for the frontend application. Components are the building blocks of the user interface.

## Files

- `Header.tsx`: The header component.
- `Footer.tsx`: The footer component.
- `VideoPlayer.tsx`: The video player component.

## Usage

Components should be used to create reusable UI elements that can be composed to build the application's interface.
